# UteRun Townsville — Backend Dependencies
# Only packages actually imported by server.py and emailer.py

# Web framework
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
python-multipart==0.0.32

# Database
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0

# Auth & validation
passlib==1.7.4
bcrypt==4.1.3
pydantic==2.13.4
pydantic_core==2.46.4
email-validator==2.3.0
PyJWT==2.13.0

# Payments
stripe==14.4.1

# SMS (OTP)
twilio==9.10.9

# Email (transactional)
resend==2.32.2

# Config
python-dotenv==1.2.2

# WebSocket
websockets==16.0

# HTTP (used by stripe/twilio/resend SDKs transitively)
httpx==0.28.1
requests==2.34.2

# Testing (move to requirements-dev.txt for production)
pytest==9.1.1
pytest-asyncio==0.23.8
